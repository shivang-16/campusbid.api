import { Request, Response, NextFunction } from "express";

export const spill = async (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        console.log(error)
    }
}